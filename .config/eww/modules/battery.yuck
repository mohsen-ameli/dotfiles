(defwidget battery []
  (revealer :reveal {matches(curr_window,"battery")}
    (box :class "control-container" :orientation "v" :space-evenly false :spacing 8
      (label :markup {battery_info} :class "control-box")
    )
  )
)
